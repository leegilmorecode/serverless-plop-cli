import { logger } from '@shared';

export async function {{camelCase name}}UseCase(
  id: string
): Promise<void> {
  // TODO - delete using secondary adapter

  logger.info(`{{camelCase useCaseSchemaName}} deleted with id: ${id}`);
}
